# Java Practice Repository

This repository is a practice project for learning how to generate code with AI assistance and make development easier.

It includes a simple `HelloWorld.java` demo and a `User` class with `name`, `age`, and `rollNumber`, plus overridden `equals` and `hashCode` behavior.

## Run

```bash
javac HelloWorld.java
java HelloWorld
```
